<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gooagoo.dao.generator.shop.SectionLineInfoMapper" >
  <resultMap id="BaseResultMap" type="com.gooagoo.entity.generator.shop.SectionLineInfo" >
    <id column="line_id" property="lineId" jdbcType="CHAR" />
    <result column="map_id" property="mapId" jdbcType="CHAR" />
    <result column="svg_tag_code" property="svgTagCode" jdbcType="VARCHAR" />
    <result column="weight" property="weight" jdbcType="INTEGER" />
    <result column="length" property="length" jdbcType="DOUBLE" />
    <result column="first_point_x" property="firstPointX" jdbcType="DOUBLE" />
    <result column="first_point_y" property="firstPointY" jdbcType="DOUBLE" />
    <result column="second_point_x" property="secondPointX" jdbcType="DOUBLE" />
    <result column="second_point_y" property="secondPointY" jdbcType="DOUBLE" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
  </resultMap>

<!-- 查询where部分sql限制条件集合 -->
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim  prefixOverrides="and" prefix="(" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="("  close=")" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

<!-- select部分查询字段集合 -->
  <sql id="Base_Column_List">
      line_id, map_id, svg_tag_code, 
      weight, length, first_point_x, 
      first_point_y, second_point_x, second_point_y, 
      create_time
  </sql>

  <!-- 按对象可分页可去重查询 -->
  <select id="selectByExample" parameterType="com.gooagoo.entity.generator.shop.SectionLineInfoExample" resultMap="BaseResultMap">
    select
      <if test="distinct">
         distinct
      </if>
      <include refid="Base_Column_List" />
    from section_line_info
    <if test="_parameter != null">
       <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
       order by ${orderByClause}
    </if>
    <if test="limitStart &gt;= 0 and limitEnd &gt;= 0">
       limit ${limitStart} , ${limitEnd}
    </if>
  </select>

  <!-- 按对象查询数据条数 -->
  <select id="countByExample" parameterType="com.gooagoo.entity.generator.shop.SectionLineInfoExample" resultType="java.lang.Integer">
    select count(*) from section_line_info
    <if test="_parameter != null">
       <include refid="Example_Where_Clause" />
    </if>
  </select>

  <!-- 按主键查询表信息 -->
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="com.gooagoo.entity.generator.shop.SectionLineInfoKey" >
    select 
      line_id, map_id, svg_tag_code, 
      weight, length, first_point_x, 
      first_point_y, second_point_x, second_point_y, 
      create_time
    from section_line_info where line_id = #{lineId,jdbcType=CHAR}
  </select>

</mapper>
