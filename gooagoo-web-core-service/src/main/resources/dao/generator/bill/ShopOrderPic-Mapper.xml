<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gooagoo.dao.generator.bill.ShopOrderPicMapper" >
  <resultMap id="BaseResultMap" type="com.gooagoo.entity.generator.bill.ShopOrderPic" >
    <id column="shop_order_detail_id" property="shopOrderDetailId" jdbcType="CHAR" />
    <result column="shop_order_id" property="shopOrderId" jdbcType="CHAR" />
    <result column="pic_url" property="picUrl" jdbcType="CHAR" />
  </resultMap>

<!-- 查询where部分sql限制条件集合 -->
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach  collection="criterion.value" item="listItem" open="(" close=")" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

<!-- 修改where部分sql限制条件集合 -->
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach  collection="criterion.value" item="listItem" open="(" close=")" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

<!-- select部分查询字段集合 -->
  <sql id="Base_Column_List">
      shop_order_detail_id, shop_order_id, pic_url
  </sql>

  <!-- 按对象可分页可去重查询 -->
  <select id="selectByExample" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPicExample" resultMap="BaseResultMap">
    select
      <if test="distinct">
         distinct
      </if>
      <include refid="Base_Column_List" />
    from shop_order_pic
    <if test="_parameter != null">
       <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
       order by ${orderByClause}
    </if>
    <if test="limitStart &gt;= 0 and limitEnd &gt;= 0">
       limit ${limitStart} , ${limitEnd}
    </if>
  </select>

  <!-- 按对象查询数据条数 -->
  <select id="countByExample" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPicExample" resultType="java.lang.Integer">
    select count(*) from shop_order_pic
    <if test="_parameter != null">
       <include refid="Example_Where_Clause" />
    </if>
  </select>

  <!-- 按主键实体查询表信息 -->
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPicKey" >
    select 
      shop_order_detail_id, shop_order_id, pic_url
    from shop_order_pic where shop_order_detail_id = #{shopOrderDetailId,jdbcType=CHAR}
  </select>

  <!-- 按对象删除数据 -->
  <delete id="deleteByExample" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPicExample">
    delete from shop_order_pic
    <if test="_parameter != null">
       <include refid="Example_Where_Clause" />
    </if>
  </delete>

  <!-- 按表主键删除数据 -->
  <delete id="deleteByPrimaryKey" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPicKey" >
    delete from shop_order_pic where shop_order_detail_id = #{shopOrderDetailId,jdbcType=CHAR}
  </delete>

  <!-- 可选择性添加数据 -->
  <insert id="insertSelective" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPic" >
    insert into shop_order_pic
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test= "shopOrderDetailId != null">
         shop_order_detail_id,
      </if>
      <if test= "shopOrderId != null">
         shop_order_id,
      </if>
      <if test= "picUrl != null">
         pic_url,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test= "shopOrderDetailId != null">
         #{shopOrderDetailId,jdbcType=CHAR},
      </if>
      <if test= "shopOrderId != null">
         #{shopOrderId,jdbcType=CHAR},
      </if>
      <if test= "picUrl != null">
         #{picUrl,jdbcType=CHAR},
      </if>
    </trim>
  </insert>

  <!-- 按对象更新表所有字段信息（create_time,c_time_stamp字段除外） -->
  <update id="updateAllByExample">
    update shop_order_pic
    <set>
        shop_order_detail_id = #{record.shopOrderDetailId,jdbcType=CHAR},
        shop_order_id = #{record.shopOrderId,jdbcType=CHAR},
        pic_url = #{record.picUrl,jdbcType=CHAR}
    </set>
    <if test="_parameter != null">
       <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>

  <!-- 按对象选择性更新表字段信息（create_time,c_time_stamp字段除外） -->
  <update id="updateByExampleSelective">
    update shop_order_pic
    <set>
      <if test="record.shopOrderDetailId !=null">
        shop_order_detail_id = #{record.shopOrderDetailId,jdbcType=CHAR},
      </if>
      <if test="record.shopOrderId !=null">
        shop_order_id = #{record.shopOrderId,jdbcType=CHAR},
      </if>
      <if test="record.picUrl !=null">
        pic_url = #{record.picUrl,jdbcType=CHAR}
      </if>
    </set>
    <if test="_parameter != null">
       <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>

  <!-- 按主键更新表中所有字段信息（create_time,c_time_stamp字段除外）-->
  <update id="updateByPrimaryKeySelective" parameterType="com.gooagoo.entity.generator.bill.ShopOrderPic" >
    update shop_order_pic
    <set>
      <if test="shopOrderDetailId !=null">
      shop_order_detail_id = #{shopOrderDetailId,jdbcType=CHAR},
      </if>
      <if test="shopOrderId !=null">
      shop_order_id = #{shopOrderId,jdbcType=CHAR},
      </if>
      <if test="picUrl !=null">
      pic_url = #{picUrl,jdbcType=CHAR}
      </if>
    </set>
    where shop_order_detail_id = #{shopOrderDetailId,jdbcType=CHAR}
  </update>

</mapper>
